<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.doctor_fish_back.repository.CommentMapper">

    <insert id="save">
        insert into comment_tb
        values(default, #{reviewId}, #{userId}, #{content}, now(), now())
    </insert>

    <update id="modifyById">
        update comment_tb
        set
            content = #{content},
            update_date = now()
        where
            id = #{id}
    </update>

    <delete id="deleteById">
        delete
        from
            comment_tb
        where
            id = #{id}
    </delete>

    <select id="findAllByReviewId" resultType="com.project.doctor_fish_back.entity.Comment">
        select
            id,
            review_id as reviewId,
            user_id as userId,
            content,
            register_date as registerDate,
            update_date as updateDate
        from
            comment_tb
        where
            review_id = #{reviewId}
    </select>

    <select id="getCommentCountByReviewId" resultType="java.lang.Long">
        select
            count(*)
        from
            comment_tb
        where
            review_id = #{reviewId}
    </select>

    <select id="findById" resultType="com.project.doctor_fish_back.entity.Comment">
        select
            id,
            review_id as reviewId,
            user_id as userId,
            content,
            register_date as registerDate,
            update_date as updateDate
        from
            comment_tb
        where
            id = #{id}
    </select>

</mapper>