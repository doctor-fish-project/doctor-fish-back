<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.doctor_fish_back.repository.LeaveMapper">

    <insert id="save">
        insert into leave_tb
        values(default, #{doctorId}, default, #{leaveDate}, #{endDate}, now(), now())
    </insert>

    <update id="modify">
        update leave_tb
        set
            leave_date = #{leaveDate},
            end_date = #{endDate},
            update_date = now()
        where
            id = #{id}
    </update>

    <update id="acceptById">
        update leave_tb
        set
            status = 2
        where
            id = #{id}
    </update>

    <update id="cancelById">
        update leave_tb
        set
            status = 3
        where
            id = #{id}
    </update>

    <delete id="deleteById">
        delete
        from
            leave_tb
        where
            id = #{id}
    </delete>

    <select id="getAllToDoctor" resultType="com.project.doctor_fish_back.entity.Leave">
        select
            id,
            doctor_id as doctorId,
            status,
            leave_date as leaveDate,
            end_date as endDate,
            register_date as registerDate,
            update_date as updateDate
        from
            leave_tb
        where
            doctor_id = #{doctorId}
    </select>

    <select id="getCountAllToDoctor" resultType="java.lang.Integer">
        select
            count(*)
        from
            leave_tb
        where
            doctor_id = #{doctorId}
    </select>

    <select id="getAllToInfo" resultType="com.project.doctor_fish_back.entity.Leave">
        select
            id,
            doctor_id as doctorId,
            status,
            leave_date as leaveDate,
            end_date as endDate,
            register_date as registerDate,
            update_date as updateDate
        from
            leave_tb
    </select>

    <select id="getCountAllToInfo" resultType="java.lang.Integer">
        select
            count(*)
        from
            leave_tb
    </select>

    <select id="findLeaveById" resultType="com.project.doctor_fish_back.entity.Leave">
        select
            id,
            doctor_id as doctorId,
            status,
            leave_date as leaveDate,
            end_date as endDate,
            register_date as registerDate,
            update_date as updateDate
        from
            leave_tb
        where
            id = #{id}
    </select>

</mapper>